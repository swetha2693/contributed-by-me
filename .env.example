# Supabase — Lab Results Explainer project (`lab explainer results`)
# Safe to expose to the browser (publishable / anon key, protected by RLS).
NEXT_PUBLIC_SUPABASE_URL=https://bhfkmltivtsxdptivzrc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_k9y6kfTPVKLlVd0phIJQFQ_mcO-5JF-

# Server-only secrets — set these in Vercel Project Settings, never commit real values.
# SUPABASE_SERVICE_ROLE_KEY=   # used for audit_event writes / retention; bypasses RLS
# ANTHROPIC_API_KEY=           # for the Marker Explainer & Report Summariser (PRD 5)
